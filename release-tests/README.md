# Release Tests

This module is used to test the released Tai-e artifacts, e.g., launching
Tai-e, ensuring that logs are written correctly, verifying that standard output
is produced, checking that the Tai-e version can be obtained, and verifying
the documentation ZIP structure.
