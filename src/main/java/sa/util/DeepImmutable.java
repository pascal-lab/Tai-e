package sa.util;

/**
 * Tagging interface for deeply immutable classes.
 *
 * Implementations should always return the same hash codes for the
 * same instances.
 */
public interface DeepImmutable {
}
